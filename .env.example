# ArangoDB Connection Settings
ARANGO_HOST=http://localhost:8529
ARANGO_USER=root

# Use environment variables for all DB credentials. Do not hardcode sensitive values.
ARANGO_PASSWORD=changeme
ARANGO_DB=demo_db

# API Settings
API_PORT=8000
LOG_LEVEL=INFO

# Security (uncomment if implementing authentication)
# SECRET_KEY=your-secret-key-here
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS Settings (uncomment if needed)
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Optional: Uncomment if using Poetry for dependency managemen
POETRY_VIRTUALENVS_IN_PROJECT=true
